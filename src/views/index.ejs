<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
</head>
<body onload="onCarregar()">
    <h1>PokeDex - Home</h1>
    <% results.forEach(pokemon => { %>
    <div>
        <p>Nome: <%= pokemon.name %></p>
        
        <a href="http://localhost:3000/detalhar/<%= pokemon.name%>">Mais Detalhes</a>
    </div>
    <% }) %>
</body>
</html>